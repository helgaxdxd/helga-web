---
layout: base.njk
title: 0xHELGA // Home
---

<main class="bento-grid">
  
  <section class="bento-item presentation">
    <div class="header-meta">SESSION_ID: @0xHELGA_CORE</div>
    <h1>Helga.</h1>
    <p>Engineering systems, capturing ephemeral moments. ⊹ 🎀</p>
  </section>

  <section class="bento-item the-vault">
    <h3>🔐 The Vault</h3>
    <div class="vault-content">
      {% for post in collections.cryptography %}
        <a href="{{ post.url }}">> {{ post.data.title }}</a>
      {% else %}
        <p class="empty-status">Ciphers pending...</p>
      {% endfor %}
    </div>
  </section>

  <section class="bento-item input-log">
    <h3>📚 Input_Log</h3>
    <div class="log-list">
      {% for entry in collections.input_log %}
        <div class="log-entry">
          <span class="mono">{{ entry.date | dateFilter }}</span>
          <a href="{{ entry.url }}">{{ entry.data.title }}</a>
        </div>
      {% endfor %}
    </div>
  </section>

  <section class="bento-item heart-protocol">
    <h3>🎀 Heart Protocol</h3>
    <div class="links-zone">
      <p>Curated tools & essentials.</p>
      </div>
  </section>

  <section class="bento-item photo-reel">
    <h3>🎞️ Moments Reel</h3>
    <div class="reel-grid">
      <div class="photo-slot">[PHOTO_01]</div>
    </div>
  </section>

  <section class="bento-item moms-project">
    <h3>🏠 Mom's Project</h3>
    <div class="family-support">
      <p>Handmade Goods & More</p>
      <a href="#" class="support-btn">Visit Shop</a>
    </div>
  </section>

  <section class="bento-item hall-of-fame">
    <h3>🎖️ Hall of Fame</h3>
    <div class="pillars">
      {% for person in collections.hall_of_fame %}
        <span class="pill">{{ person.data.title }}</span>
      {% endfor %}
    </div>
  </section>

</main>