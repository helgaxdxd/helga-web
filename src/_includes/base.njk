<style>
    body { 
        background-color: #FDFCF8; /* Crema suave */
        color: #4A4A4A; /* Gris carbón suave, no negro */
        font-family: 'Outfit', sans-serif; /* Una fuente más redonda y amigable */
    }
    .glass { 
        background: rgba(255, 255, 255, 0.7); 
        backdrop-filter: blur(10px); 
        border: 2px solid #EFEBE4; /* Bordes color arena */
        border-radius: 40px; /* Bordes mucho más redondeados */
        box-shadow: 0 10px 30px rgba(0,0,0,0.03); /* Sombras muy tenues */
    }
    .accent-text { color: #D4A373; /* Terracota Boho */ }
</style>